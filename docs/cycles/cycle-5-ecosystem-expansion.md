# 周期 5 开发文档 — 多端与生态扩展

> **项目**: My Application — Android 音乐播放器  
> **周期定位**: 周期 5 / 建议 2 周  
> **对应总文档**: `docs/technical-implementation.md`

---

## 1. 周期目标

本周期开始从“播放器”扩展到“多端入口 + 内容生态外壳”，把 Widget、驾驶模式、Android Auto、MV 和社区能力逐步铺开。

---

## 2. 范围定义

### 2.1 前端范围

| 模块 | 任务 | 结果 |
|------|------|------|
| 桌面入口 | 桌面小组件 | 支持歌曲信息展示和基础播放控制 |
| 大屏场景 | 驾驶模式 | 支持大按钮、大字体、简化交互 |
| 车机场景 | Android Auto | 支持媒体浏览树和播放控制 |
| 视频内容 | MV / 视频播放 | 支持进入视频播放页和全屏播放 |
| 社区内容 | 动态 / 社区外壳 | 先完成列表、详情和分页骨架 |

### 2.2 后端范围

| 模块 | 任务 | 结果 |
|------|------|------|
| 视频能力 | `getMvUrl` | 返回 MV 可播放地址和必要元数据 |
| 社区能力 | Feed / 详情 / 评论接口 | 支撑社区列表和详情页 |
| 多端数据 | 媒体元数据完整性 | 保证 Widget / Auto / 视频页所需字段完整 |

---

## 3. 前端开发清单

### 3.1 桌面小组件

**涉及文件**

- `widget/MusicWidgetProvider.kt`
- `widget/MusicWidgetReceiver.kt`
- `res/xml/music_widget_info.xml`

**开发要求**

- 优先采用 `Glance`
- 支持显示封面、标题、歌手和基础控制按钮
- 播放状态变化时及时刷新

### 3.2 驾驶模式

**涉及文件**

- `feature/driving/DrivingModeScreen.kt`
- `feature/driving/DrivingModeViewModel.kt`

**开发要求**

- 复用现有播放控制，不另造控制链
- UI 以大按钮、大标题为主
- 可选预留语音控制入口，但本周期不强求完整语音方案

### 3.3 Android Auto

**涉及文件**

- `MusicPlaybackService.kt`
- `AndroidManifest.xml`
- `automotive_app_desc.xml`

**开发要求**

- `MusicPlaybackService` 需具备 `MediaLibraryService` 能力
- 返回基础媒体浏览树：收藏、最近播放、歌单
- 需要真机或模拟器验证

### 3.4 MV / 视频播放

**涉及文件**

- `feature/player/VideoPlayerScreen.kt`
- `media/video/VideoPlayerManager.kt`

**开发要求**

- 支持从播放页更多菜单进入
- 支持全屏、横屏和退出回到音频播放页
- 视频播放异常不能拖垮主音频服务

### 3.5 动态 / 社区外壳

**涉及模块**

- `feature/community` 预留模块

**开发要求**

- 先完成 Feed 列表、详情、空态、错误态
- 评论、点赞、分享等交互是否在本周期完整落地，取决于后端接口成熟度

---

## 4. 后端开发清单

### 4.1 MV 接口

**接口要求**

- `getMvUrl(platform, songId)` 或等价能力
- 返回视频播放地址、封面、清晰度信息、时长等基础字段

### 4.2 社区接口

**接口要求**

- Feed 列表
- 动态详情
- 评论列表
- 分页参数与排序约定
- 鉴权和匿名访问策略

### 4.3 多端数据支持

**需要确认**

- Widget / Auto 所需的封面、标题、时长、播放状态字段是否完整
- 社区富文本或多媒体字段是否需要额外约束

---

## 5. 周内安排建议

| 时间 | 前端重点 | 后端重点 | 联调重点 |
|------|----------|----------|---------|
| 第 1 周前半 | Widget、驾驶模式骨架 | MV 与社区接口契约 | 多端字段确认 |
| 第 1 周后半 | Android Auto、MV 页 | 视频接口、Feed 接口 | MV 播放联调 |
| 第 2 周前半 | 社区外壳与分页 | 评论和详情能力 | 社区列表与详情联调 |
| 第 2 周后半 | 回归和缺陷修复 | 稳定性修正 | 多端整体验收 |

---

## 6. 联调清单

- Widget 状态刷新和控制行为正确
- Android Auto 能返回可浏览的媒体树
- MV 页面能正常进入、播放、退出
- 社区列表和详情的分页、空态、错误态完整

---

## 7. 验收标准

- Widget 至少完成一个稳定规格
- Android Auto 完成真实环境验证
- MV 链路可用，不影响现有纯音频播放
- 社区模块至少完成只读浏览闭环

---

## 8. 风险与依赖

- Android Auto 没有真实测试环境就不能认定完成
- 社区接口一旦频繁变更，前端会被拖进反复重构
- MV 与音频播放并存时，要防止音频焦点和生命周期冲突

---

## 9. 周期产出物

- 可用的 Widget
- 驾驶模式入口
- Android Auto 支持
- MV 播放能力
- 社区内容浏览骨架
